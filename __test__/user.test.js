import supertest from "supertest";
import app from "../server";
// import { Sequelize } from "sequelize";
import sequelize from "../config";

describe("User", () => {
  describe("user registration", () => {
    it("should return the user palyloa", async () => {});
  });
});
